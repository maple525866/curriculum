<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.curriculum.mapper.StudentMapper">

    <resultMap id="StudentResultMap" type="Student">
        <id property="studentId" column="student_id"/>
        <result property="studentName" column="student_name"/>
        <result property="studentNo" column="student_no"/>
        <result property="classId" column="class_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="className" column="class_name"/>
        <result property="gradeName" column="grade_name"/>
    </resultMap>

    <select id="selectAll" resultMap="StudentResultMap">
        SELECT s.*, c.class_name, g.grade_name
        FROM student s
        LEFT JOIN class c ON s.class_id = c.class_id
        LEFT JOIN grade g ON c.grade_id = g.grade_id
        ORDER BY s.student_id
    </select>

    <select id="selectById" resultMap="StudentResultMap">
        SELECT s.*, c.class_name, g.grade_name
        FROM student s
        LEFT JOIN class c ON s.class_id = c.class_id
        LEFT JOIN grade g ON c.grade_id = g.grade_id
        WHERE s.student_id = #{studentId}
    </select>

    <select id="selectByStudentNo" resultMap="StudentResultMap">
        SELECT s.*, c.class_name, g.grade_name
        FROM student s
        LEFT JOIN class c ON s.class_id = c.class_id
        LEFT JOIN grade g ON c.grade_id = g.grade_id
        WHERE s.student_no = #{studentNo}
    </select>

    <insert id="insert" parameterType="Student" useGeneratedKeys="true" keyProperty="studentId">
        INSERT INTO student (student_name, student_no, class_id)
        VALUES (#{studentName}, #{studentNo}, #{classId})
    </insert>

    <update id="update" parameterType="Student">
        UPDATE student
        SET student_name = #{studentName},
            student_no = #{studentNo},
            class_id = #{classId}
        WHERE student_id = #{studentId}
    </update>

    <delete id="deleteById">
        DELETE FROM student WHERE student_id = #{studentId}
    </delete>

</mapper>

